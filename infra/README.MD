# infra

This directory contains local infrastructure assets required to run the `Scaffold` application.

It is intended to contain:
- reverse proxy configuration,
- supporting files for the development environment,
- local HTTP/HTTPS routing settings.

The currently planned subdirectory is `traefik`.