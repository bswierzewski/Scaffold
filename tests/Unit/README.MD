# tests/Unit

This directory is intended for the `Scaffold.Tests.Unit` project.

It is expected to contain:
- `Scaffold.Tests.Unit.csproj` - an `xunit` or `nunit` test project.
- fast unit tests for business logic.
- tests for services, validators, and other module elements that do not require full infrastructure.