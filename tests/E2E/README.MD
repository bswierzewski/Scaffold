# tests/E2E

This directory is intended for the `Scaffold.Tests.E2E` project.

It is expected to contain:
- `Scaffold.Tests.E2E.csproj` - an `xunit` or `nunit` test project.
- references to AppHost.
- end-to-end tests covering the full deployed application, including the API, database, and component integrations.