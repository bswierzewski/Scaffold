# tests/Integration

This directory is intended for the `Scaffold.Tests.Integration` project.

It is expected to contain:
- `Scaffold.Tests.Integration.csproj` - an `xunit` or `nunit` test project.
- EF Core tests.
- persistence layer tests.
- database integration tests, for example by using Testcontainers.