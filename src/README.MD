# src

This directory contains all source code for the `Scaffold` application.

Responsibility split:
- `Aspire` - service orchestration and shared hosting defaults.
- `Backend` - the API, domain modules, and shared libraries.
- `Frontend` - the web application.