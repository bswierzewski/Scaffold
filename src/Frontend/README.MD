# src/Frontend

This directory contains the frontend for the `Scaffold` application.

It is intended to contain:
- a Vite-based application,
- API client generation configuration,
- UI components and frontend application logic.

The currently planned application subdirectory is `app`.