# src/Aspire

This directory contains projects responsible for local orchestration and shared defaults within the .NET Aspire ecosystem.

It is intended to include:
- `AppHost` - an Aspire AppHost project that wires up environment dependencies.
- `ServiceDefaults` - a library that shares common service defaults.