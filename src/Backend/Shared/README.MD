# src/Backend/Shared

This directory contains libraries shared by the backend host and modules.

Responsibility split:
- `Core` - lightweight contracts and base types without heavy dependencies.
- `Infrastructure` - shared infrastructure and integration extensions.